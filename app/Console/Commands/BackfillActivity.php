<?php

// BackfillActivity removed - auditing is now handled by ReferenceCrud.
// Keeping this stub prevents accidental `activity:backfill` calls.

// If you want to re-enable a backfill tool, recreate this command with
// the desired behavior. Previously it populated activity_log for existing
// records; we no longer require it in the codebase.

return;
